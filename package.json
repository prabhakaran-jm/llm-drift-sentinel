{
  "name": "llm-drift-sentinel",
  "version": "0.1.0",
  "description": "LLM Drift & Abuse Sentinel - Monitor LLM applications on Vertex AI/Gemini, detect drift and abuse, push metrics to Datadog",
  "license": "MIT",
  "private": false,
  "workspaces": [
    "services/*",
    "web/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.2.2"
  }
}

